<h1>Messages</h1>

<table>
  <thead>
    <tr>
      <% Message.attribute_names.each do |attr| %>
        <th><%= attr.titleize %></th>
      <% end %>
      <th>Show Button</th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <% Message.attribute_names.each do |attr| %>
          <td><%= message.send(attr) %></td>
        <% end %>
        <td><%= link_to 'Show', message_path(message) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
